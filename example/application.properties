# rust-spring example configuration
server.port=9090
app.name=my-rust-app
app.version=2.0.0
app.max-connections=200
# ConditionalOnProperty 演示：只有该属性为 true 时 CacheService 才会被注册
feature.cache.enabled=true
# feature.analytics.enabled 未设置，AnalyticsService 不会被注册

# volunteer query demo
volunteer.db.url=mysql://root:password@127.0.0.1:3306/volunteer_duration
